# Overview
An implementation of [[Updatable]].

Stores all objects in the scene and manages the main update loop of the program.

It also builds the [[ScreenBuffer]] and calls physics updates for [[Gravitational]] and handles collision via [[CollisionHandler]].