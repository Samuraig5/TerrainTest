# Overview
An extension of [[Scene]].

Handles the creation, loading and storing of [[TerrainVolume|TerrainVolumes]].

It uses the [[OpenSimplex2S]] algorithm to generate procedural height offsets for terrain generation.